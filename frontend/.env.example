VITE_GEMINI_API_KEY=#
VITE_SUPABASE_URL=#
VITE_SUPABASE_ANON_KEY=#
# For Backend
SUPABASE_PROJECT_ID=#
# You still need to add these for AI features to work securely if we use a service role,
# but for now, we'll verify the user's JWT.
# Cloudinary Credentials (Required for Receipt & Profile Uploads)
CLOUDINARY_CLOUD_NAME="your-cloud-name"
CLOUDINARY_API_KEY="your-api-key"
CLOUDINARY_API_SECRET="your-api-secret"

# Voice Assistant
VITE_ELEVENLABS_AGENT_ID="your-agent-id"

# Backend URL
VITE_API_URL="http://localhost:3000"
VITE_SITE_URL="http://localhost:5173"
